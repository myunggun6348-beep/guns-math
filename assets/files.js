/* =========================================================
   내려받는 파일 목록 — 이제는 여기 손으로 적지 않습니다.

   자료실(files.html)에서 선생님이 ?admin 으로 들어가 올리면 사이트
   저장소(api/files.js)에 바로 들어가고, 홈의 찾기도 그걸 함께 읽습니다
   (index.html 에서 /api/files 를 따로 불러옵니다).

   이 배열은 비워 둔 채로 둡니다 — 저장소를 아직 안 만들었을 때를 대비한
   빈 기본값일 뿐입니다.
   ========================================================= */
const FILES = [];
